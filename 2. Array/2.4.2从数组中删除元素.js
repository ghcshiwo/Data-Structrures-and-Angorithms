// pop() 方法可以删除数组末尾的元素
// 将删掉的元素作为方法的返回值返回
{
  let nums = [1,2,3,4,5,9]
  console.log(nums.pop()) // 9
  console.log(nums) // [1,2,3,4,5]
}

// shift() 方法可以删除数组的第一个元素
// 将删掉的元素作为方法的返回值返回
{
  let nums = [9,1,2,3,4,5]
  console.log(nums.shift()) // 9
  console.log(nums) // [1,2,3,4,5]
}